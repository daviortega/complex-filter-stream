{
  "name": "complex-filter-stream",
  "version": "0.1.3",
  "description": "Transform stream with complex filter for objects",
  "main": "index.js",
  "scripts": {
    "test": "NODE_PATH=node_modules node_modules/.bin/nyc node_modules/.bin/mocha --reporter mocha-pretty-bunyan-nyan $(find src -name '*tests.js')",
    "mocha": "NODE_PATH=node_modules node_modules/.bin/mocha --reporter ${MOCHA_REPORTER-spec} $(find src -name '*tests.js')",
    "coverage": "NODE_PATH=node_modules node_modules/.bin/nyc report --reporter=text-lcov | coveralls"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/daviortega/complex-filter-stream.git"
  },
  "author": "Davi Ortega",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/daviortega/complex-filter-stream/issues"
  },
  "homepage": "https://github.com/daviortega/complex-filter-stream#readme",
  "dependencies": {
    "bunyan": "^1.8.12",
    "combine-stream": "^0.0.4",
    "lodash": "^4.17.10",
    "pumpify": "^1.4.0",
    "unique-stream": "^2.2.1"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "coveralls": "^3.0.0",
    "mocha": "^5.1.1",
    "mocha-pretty-bunyan-nyan": "^1.0.4",
    "nyc": "^11.7.1"
  }
}
